# はじむ言語：例外処理
関数 割り算(a, b):
    もし b == 0 なら
        投げる "ゼロで割ることはできません"
    終わり
    戻す a / b
終わり

試行:
    変数 結果1 = 割り算(10, 2)
    表示("10 / 2 = " + 文字列化(結果1))
    
    変数 結果2 = 割り算(10, 0)
    表示("10 / 0 = " + 文字列化(結果2))
捕獲 エラー:
    表示("エラーが発生: " + エラー)
最終:
    表示("処理完了")
終わり
