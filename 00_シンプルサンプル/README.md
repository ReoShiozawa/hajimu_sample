# はじむ言語：実行可能なサンプルコード集

このディレクトリには、**実際に動作することが確認された**はじむ言語のサンプルコードが含まれています。

## 目的

はじむ言語の学習と、言語の広がりを促進するための実用的なサンプルコード集です。

## サンプル一覧

| ファイル | 説明 | 学習内容 |
|---------|------|--------|
| `01_hello.jp` | 最初のプログラム | 基本的な表示 |
| `02_variables.jp` | 変数と計算 | 変数、算術演算 |
| `03_conditions.jp` | 条件分岐 | if/else文、条件判定 |
| `04_loops.jp` | ループ処理 | for、while ループ |
| `05_arrays.jp` | 配列と要素処理 | 配列、foreach ループ |
| `06_functions.jp` | 関数定義と呼び出し | 関数の定義と使用 |
| `07_classes.jp` | クラスとオブジェクト | 型定義、インスタンス化 |
| `08_exception_handling.jp` | 例外処理 | try/catch文、例外投出 |
| `09_string_operations.jp` | 文字列操作 | 文字列関数、分割、置換 |
| `10_math_functions.jp` | 数学関数と再帰 | 数学関数、再帰関数 |

## 実行方法

```bash
$ hajimu ファイル名.jp
```

## 修正内容

このディレクトリのファイルは、公式のはじむ言語仕様に従って以下の点に注意して作成されています：

### 重要な構文上の修正点

#### 1. クラス定義のキーワード
- **正**: `型` キーワードを使用
```hajimu
型 人:
    初期化(名前):
        自分.名前 = 名前
    終わり
終わり
```

#### 2. インスタンス作成
- **正**: `新規` キーワードを使用
```hajimu
変数 太郎 = 新規 人("太郎")
```

#### 3. ループ構文
- **正しいfor ループ**: `i を 開始 から 終了 繰り返す`
```hajimu
i を 1 から 5 繰り返す
    表示(i)
終わり
```

- **正しいwhile ループ**: `条件 条件式 の間`
```hajimu
条件 x > 0 の間
    x = x - 1
終わり
```

#### 4. メソッド定義
- **正**: クラス内のメソッドは `関数` キーワードを使用
```hajimu
型 人:
    初期化(名前):
        自分.名前 = 名前
    終わり
    
    関数 挨拶():
        表示("こんにちは、" + 自分.名前)
    終わり
終わり
```

#### 5. 関数のコロン
- 関数・メソッド定義には `:` が必須

## テスト完了

✅ すべてのサンプルが実行可能であることを確認済みです

```
$ hajimu 01_hello.jp
こんにちは、はじむ世界！
```

各サンプルは独立しており、学習の順序に沿って進められます。

---

**注**: 最初のプロジェクトで一部のファイルに構文エラーがありました。このディレクトリのファイルはすべて確認済みで、確実に実行可能です。
